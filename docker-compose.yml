version: "3.8"

services:
  db:
    image: postgres:14
    container_name: go_practice4_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: practice4db
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
